this document contains screenshots for TABLES USING \dt table name to see their columns and constraints.
Then using 'select * from tablename;' to see data inserted in the tables .
The other for queries from 1 to 20 using (inner-join, left-join, right-join, sum(),having(),count() ) in the queries.
Their is the pgdump  sql file for the details of this all database  contains all information on 20 tables 
format of the screenshots:
1-2: \l and \dt
taxpayer to revenue-target :20 screenshots with it's name on each png to see their constraints and their columns  of the table. 
from(tablename) png's or screenshots are for displaying  data inside the tables using the query (select * from tablename; ) eg select * from taxpayer;
query(number of query) this are the screenshots for the query in the assignment from 1 to 20 
there was a last document with the  sql format that contains the details of the database


